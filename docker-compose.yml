version: '3.8'

services:
  actiboost-negotiator:
    build: .
    container_name: actiboost-negotiator
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped